<h2>Sign Up Confirmation</h2>

<h4>Are you sure these values are correct?</h4>
	
<div>Email: <%= @user.email %></div>
<div>Name: <%= @user.name %></div>

<%= button_to "Yes", {controller: "users", action: "create", params: { user: params.require(:user).permit(:name, :email, :password, :password_confirmation).to_h }}, class: 'btn btn-success' %>

<%= button_to "No", {controller: "users", action: "new"}, class: 'btn btn-failure', method: :get %>


	